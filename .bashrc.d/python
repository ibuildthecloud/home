if [ -e ~/.local/bin/startup.py ]; then
    export PYTHONSTARTUP=~/.local/bin/startup.py
fi

alias python=python3

PYSHELL="${HOME}/.virtualenvs/shell/bin/activate"
if [ -e ${PYSHELL} ]; then
    VIRTUAL_ENV_DISABLE_PROMPT=true source $PYSHELL
fi
